apiVersion: v1
kind: Service
metadata:
    name: lyft-client
    labels:
        type: api-client
        client: lyft
spec:
    ports:
        - port: 80
        - port: 443
    selector:
        type: api-client
        client: lyft
